<!DOCTYPE html>
<html>

<head>
  <title>Outfit Options</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+Display:ital,wght@0,100..900;1,100..900&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1 class="noto-serif-display-bold">Fashion House👖👗</h1>

  <form action="/fashion" method="POST" class="buttons">
    <button type="submit" value="trad" name="choice">👘👳🏽‍♂️</button>
    <button type="submit" value="casual" name="choice">👕👟</button>
    <button type="submit" value="corporate" name="choice">👔👞</button>
  </form>
  <div id="outfitContainer" class="hidden noto-serif-display-normal">

    <!-- If no outfit option yet, show: 'pick your Outfit type'-->
    <!-- Else, show the oufit name e.g. <h2>Fashion name</h2>-->
    <% if (locals.outfitOption){ %>
      <h2>
        <%= outfitOption.name %>
      </h2>
      <h3>Materials:</h3>
      <ul id="outfitList">
        <li>
          <%= outfitOption.materials.head.name %>, <%= outfitOption.materials.head.position%>
        </li>
        <li>
          <%= outfitOption.materials.body.name %>
        </li>
        <% outfitOption.materials.foot.forEach((footwear)=> { %>
          <li>
            <%= footwear.quantity %> of <%= footwear.name %>
          </li>
          <% }) %>
            <h3>Footwear Materials:</h3>
            <% outfitOption.materials.foot.forEach((footwear)=> { %>
              <li>
                <%= footwear.materials%>
              </li>
              <% }) %>
      </ul>
      <% } else { %>
        <h2>Pick your favourite type of outfit 👆</h2>
        <% } %>



  </div>

  <footer>
    Copyright © Abdulraheem Habib Abiola, <%= new Date().getFullYear() %>
  </footer>
</body>

</html>